# Leave unset for local dev (Vite proxies /api to http://localhost:3000, see vite.config.js).
# When deploying the frontend separately from the backend (e.g. Vercel/Netlify + Railway),
# set this to the deployed backend's base URL, e.g. https://your-app.up.railway.app
VITE_API_URL=